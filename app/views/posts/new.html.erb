<header class="masthead"><!-- Vielleicht kann ich hier ein Bild reincrawlen lassen? als jeweiliges Headerbild vom Beitrag --></header>


<div class="container">
  <div class="row">
    <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
      <h1>Neuen Beitrag anlegen</h1>

      <% if @post.errors.any? %>
        <h2>Error:</h2>
        <ul>
          <% @post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
            <% end %>
        </ul>
        <% end %>
      <%= render 'posts/form' %>
    </div>

  </div>
</div>
