<!-- Navigation -->
<nav class="navbar navbar-default navbar-custom navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header page-scroll">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        Menu <i class="fa fa-bars"></i>
      </button>
      <a class="navbar-brand" <%=link_to "Meine Zeit in den USA", posts_path %></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li>
            <%= link_to "Logout", destroy_user_session_path, method: :delete %>
          </li>
          <!-- Wenn ich eingeloggt bin und auf Image-Gallery bin, dann zeig mir Uploadfunktion -->
          <% if current_page? (pages_image_gallery_path) %>
            <li>
              <a href="#">Upload</a>
            </li>
          <% end %>
          <% else %>
            <!-- Vielleicht will ich doch gar nicht, dass der User nen Anmeldebutton hat<li>
              <%= link_to "Sign in!", new_user_registration_path %>
            </li> -->
        <% end %>


        <li>
          <%= link_to "Home", posts_path %>
        </li>
        <li>
          <%= link_to "All weeks", pages_all_weeks_path %>
        </li>
        <li>
          <%= link_to "Image Gallery", pages_image_gallery_path %>
        </li>
        <li>
          <%= link_to "About", pages_about_path %>
        </li>
        <li>
          <%= link_to "Contact", pages_contact_path %>
        </li>
      </ul>
    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container -->
</nav>